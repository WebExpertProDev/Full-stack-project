from django.db import models

# Create your models here.


# An Active Record for a file that physically exists on AWS s3 or any static file server (on the cloud)
class CloudFile(models.Model):
    pass
